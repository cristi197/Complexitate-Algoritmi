---
import ChapterLayout from '../../layouts/ChapterLayout.astro';

const sections = [{"id":"deschidere","label":"Deschidere"},{"id":"citire","label":"Citire"},{"id":"scriere","label":"Scriere"},{"id":"freopen","label":"freopen"},{"id":"prelucrare","label":"Prelucrare"},{"id":"bac","label":"Probleme BAC"}];
---
<ChapterLayout
  title="Fișiere"
  subtitle="Capitolul 7 — ifstream, ofstream, fstream, freopen, citire/scriere, prelucrare fișiere și probleme BAC"
  icon="📁"
  gradient="linear-gradient(135deg,#831843 0%,#be185d 50%,#ec4899 100%)"
  tags={["Fișiere","fstream","ifstream","ofstream","EOF"]}
  sections={sections}
  chapterNum={7}
  description="Fișiere — informatică pentru liceu și bacalaureat"
>
<!-- ─── 1. DESCHIDERE FIȘIERE ────────────────────────── -->
<section id="deschidere">
  <div class="slide">
    <div class="slide-header">
      <span class="slide-icon icon-pink">📂</span>
      <h2>Deschiderea și închiderea fișierelor</h2>
    </div>
    <div class="slide-body">
      <div class="beginner-box">
        <div class="bb-title">📁 Analogie: Fișierele = Caiete de temă</div>
        <p>Un fișier text este ca un caiet: îl <strong>deschizi</strong> (fopen), <strong>citești</strong> sau <strong>scrii</strong> în el, apoi îl <strong>închizi</strong> (fclose). Dacă uiți să-l închizi, datele scrise pot fi pierdute!</p>
      </div>
      <p>În C++ folosim fluxuri din antetul <code>&lt;fstream></code> pentru a lucra cu fișiere text.</p>

      <pre><span class="kw">#include</span> <span class="st">&lt;fstream></span>
<span class="kw">using namespace</span> std;

<span class="cm">// ifstream — doar citire</span>
ifstream fin(<span class="st">"date.in"</span>);
<span class="kw">if</span> (!fin) &#123; cout &lt;&lt; <span class="st">"Eroare: fisierul nu exista!"</span>; <span class="kw">return</span> <span class="nu">1</span>; &#125;

<span class="cm">// ofstream — doar scriere (creează sau suprascrie fișierul)</span>
ofstream fout(<span class="st">"date.out"</span>);

<span class="cm">// ofstream cu append (adaugă la final)</span>
ofstream fapp(<span class="st">"log.txt"</span>, ios::app);

<span class="cm">// fstream — citire și scriere</span>
fstream fs(<span class="st">"date.txt"</span>, ios::in | ios::out);

<span class="cm">// Închiderea explicită (opțional — se face și automat la ieșire din funcție)</span>
fin.close();
fout.close();</pre>

      <div class="card-grid" style="grid-template-columns:1fr 1fr 1fr;margin:14px 0;">
        <div class="card pink">
          <strong>ifstream</strong>
          <p>Input File Stream — citire din fișier</p>
        </div>
        <div class="card blue">
          <strong>ofstream</strong>
          <p>Output File Stream — scriere în fișier</p>
        </div>
        <div class="card teal">
          <strong>fstream</strong>
          <p>File Stream — citire și scriere</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ─── 2. CITIRE DIN FIȘIER ─────────────────────────── -->
<section id="citire">
  <div class="slide">
    <div class="slide-header">
      <span class="slide-icon icon-blue">📥</span>
      <h2>Citire din fișier</h2>
    </div>
    <div class="slide-body">
      <h3 style="margin-bottom:10px;font-size:15px;">Citire token cu token (numere, cuvinte)</h3>
      <pre>ifstream fin(<span class="st">"date.in"</span>);
<span class="kw">int</span> n, x;
fin >> n;
<span class="kw">for</span> (<span class="kw">int</span> i = <span class="nu">1</span>; i &lt;= n; i++) &#123;
    fin >> x;
    <span class="cm">// procesare x</span>
&#125;
fin.close();</pre>

      <h3 style="margin:16px 0 10px;font-size:15px;">Citire linie cu linie</h3>
      <pre>ifstream fin(<span class="st">"text.txt"</span>);
string linie;
<span class="kw">while</span> (getline(fin, linie)) &#123;
    <span class="cm">// procesare linie</span>
    cout &lt;&lt; linie &lt;&lt; endl;
&#125;</pre>

      <h3 style="margin:16px 0 10px;font-size:15px;">Citire până la EOF (fără a ști numărul de elemente)</h3>
      <pre>ifstream fin(<span class="st">"numere.in"</span>);
<span class="kw">int</span> x;
<span class="kw">while</span> (fin >> x) &#123;
    <span class="cm">// procesare x până la capătul fișierului</span>
&#125;</pre>

      <h3 style="margin:16px 0 10px;font-size:15px;">Citire matrice din fișier</h3>
      <pre>ifstream fin(<span class="st">"matrice.in"</span>);
<span class="kw">int</span> n, m, a[<span class="nu">101</span>][<span class="nu">101</span>];
fin >> n >> m;
<span class="kw">for</span> (<span class="kw">int</span> i=<span class="nu">1</span>; i&lt;=n; i++)
    <span class="kw">for</span> (<span class="kw">int</span> j=<span class="nu">1</span>; j&lt;=m; j++)
        fin >> a[i][j];</pre>
    </div>
  </div>
</section>

<!-- ─── 3. SCRIERE ───────────────────────────────────── -->
<section id="scriere">
  <div class="slide">
    <div class="slide-header">
      <span class="slide-icon icon-teal">📤</span>
      <h2>Scriere în fișier</h2>
    </div>
    <div class="slide-body">
      <pre>ofstream fout(<span class="st">"rezultat.out"</span>);

<span class="cm">// Scriere numere</span>
fout &lt;&lt; n &lt;&lt; endl;
<span class="kw">for</span> (<span class="kw">int</span> i=<span class="nu">1</span>; i&lt;=n; i++)
    fout &lt;&lt; v[i] &lt;&lt; <span class="st">" "</span>;
fout &lt;&lt; endl;

<span class="cm">// Scriere formatată — aliniere la câmpuri fixe</span>
<span class="kw">#include</span> <span class="st">&lt;iomanip></span>
fout &lt;&lt; setw(<span class="nu">10</span>) &lt;&lt; setfill(<span class="st">'0'</span>) &lt;&lt; nr;   <span class="cm">// ex: 0000000042</span>
fout &lt;&lt; fixed &lt;&lt; setprecision(<span class="nu">2</span>) &lt;&lt; <span class="nu">3.14159</span>;  <span class="cm">// 3.14</span>

fout.close();</pre>

      <div class="callout success">
        <strong>Buna practică:</strong> Apelați întotdeauna <code>fout.close()</code> sau lăsați obiectul să iasă din scope — asigurați că datele sunt scrise pe disc (flush).
      </div>
    </div>
  </div>
</section>

<!-- ─── 4. FREOPEN ───────────────────────────────────── -->
<section id="freopen">
  <div class="slide">
    <div class="slide-header">
      <span class="slide-icon icon-orange">🔀</span>
      <h2>freopen — redirectare I/O (stil BAC olimpiadă)</h2>
    </div>
    <div class="slide-body">
      <p>La olimpiadele de informatică, problemele cer citire din <code>problema.in</code> și scriere în <code>problema.out</code>. Cel mai simplu mod: <strong>redirectăm stdin/stdout</strong> cu <code>freopen</code>.</p>

      <pre><span class="kw">#include</span> <span class="st">&lt;cstdio></span>   <span class="cm">// freopen</span>
<span class="kw">#include</span> <span class="st">&lt;iostream></span>
<span class="kw">using namespace</span> std;

<span class="kw">int</span> <span class="fn">main</span>() &#123;
    freopen(<span class="st">"suma.in"</span>,  <span class="st">"r"</span>, stdin);   <span class="cm">// cin va citi din suma.in</span>
    freopen(<span class="st">"suma.out"</span>, <span class="st">"w"</span>, stdout);  <span class="cm">// cout va scrie în suma.out</span>

    <span class="kw">int</span> n, s = <span class="nu">0</span>, x;
    cin >> n;
    <span class="kw">for</span> (<span class="kw">int</span> i=<span class="nu">1</span>; i&lt;=n; i++) &#123; cin >> x; s += x; &#125;
    cout &lt;&lt; s;

    <span class="kw">return</span> <span class="nu">0</span>;
&#125;</pre>

      <div class="callout info">
        <strong>Avantaj freopen:</strong> Puteți folosi <code>cin</code>/<code>cout</code> exact ca la citirea de la tastatură. Nu trebuie să schimbați restul codului.
      </div>
      <div class="callout warning">
        <strong>Alternativă cu ifstream/ofstream:</strong> Înlocuiți <code>cin</code> cu <code>fin</code> și <code>cout</code> cu <code>fout</code>. Ambele variante sunt acceptate la BAC.
      </div>
    </div>
  </div>
</section>

<!-- ─── 5. PRELUCRARE ────────────────────────────────── -->
<section id="prelucrare">
  <div class="slide">
    <div class="slide-header">
      <span class="slide-icon icon-purple">🔧</span>
      <h2>Prelucrarea fișierelor — exemple complete</h2>
    </div>
    <div class="slide-body">
      <h3 style="margin-bottom:10px;font-size:15px;">Exemplu complet: citire din fișier, prelucrare, scriere</h3>
      <pre><span class="kw">#include</span> <span class="st">&lt;fstream></span>
<span class="kw">using namespace</span> std;

<span class="kw">int</span> <span class="fn">main</span>() &#123;
    ifstream fin(<span class="st">"maxim.in"</span>);
    ofstream fout(<span class="st">"maxim.out"</span>);

    <span class="kw">int</span> n; fin >> n;
    <span class="kw">int</span> maxim, x;
    fin >> maxim;
    <span class="kw">for</span> (<span class="kw">int</span> i=<span class="nu">2</span>; i&lt;=n; i++) &#123;
        fin >> x;
        <span class="kw">if</span> (x > maxim) maxim = x;
    &#125;
    fout &lt;&lt; maxim;

    fin.close(); fout.close();
    <span class="kw">return</span> <span class="nu">0</span>;
&#125;</pre>

      <h3 style="margin:16px 0 10px;font-size:15px;">Copiere fișier text (linie cu linie)</h3>
      <pre>ifstream fin(<span class="st">"sursa.txt"</span>);
ofstream fout(<span class="st">"copie.txt"</span>);
string linie;
<span class="kw">while</span> (getline(fin, linie))
    fout &lt;&lt; linie &lt;&lt; <span class="st">"\n"</span>;</pre>

      <h3 style="margin:16px 0 10px;font-size:15px;">Numărarea liniilor și cuvintelor dintr-un fișier</h3>
      <pre>ifstream fin(<span class="st">"text.txt"</span>);
<span class="kw">int</span> nrLinii = <span class="nu">0</span>, nrCuvinte = <span class="nu">0</span>;
string linie, cuvant;
<span class="kw">while</span> (getline(fin, linie)) &#123;
    nrLinii++;
    stringstream ss(linie);
    <span class="kw">while</span> (ss >> cuvant) nrCuvinte++;
&#125;</pre>
    </div>
  </div>
</section>

<!-- ─── 6. PROBLEME BAC ─────────────────────────────── -->
<section id="bac">
  <div class="slide">
    <div class="slide-header">
      <span class="slide-icon icon-orange">📝</span>
      <h2>Probleme tip BAC — fișiere</h2>
    </div>
    <div class="slide-body">

      <div class="callout purple">
        <strong>Problemă 1 (BAC clasic):</strong> Fișierul <code>sir.in</code> conține pe prima linie un număr natural n, iar pe a doua linie n numere întregi separate prin spații. Scrieți în <code>sir.out</code> suma elementelor pare și produsul elementelor impare.
      </div>
      <div class="callout success">
        <strong>Soluție:</strong>
        <pre style="margin:8px 0 0;background:transparent;border:none;padding:0;"><span class="kw">#include</span> <span class="st">&lt;fstream></span>
<span class="kw">using namespace</span> std;
<span class="kw">int</span> <span class="fn">main</span>() &#123;
    ifstream fin(<span class="st">"sir.in"</span>);
    ofstream fout(<span class="st">"sir.out"</span>);
    <span class="kw">int</span> n, x, sumPar = <span class="nu">0</span>, prodImpar = <span class="nu">1</span>;
    fin >> n;
    <span class="kw">for</span> (<span class="kw">int</span> i=<span class="nu">1</span>; i&lt;=n; i++) &#123;
        fin >> x;
        <span class="kw">if</span> (x % <span class="nu">2</span> == <span class="nu">0</span>) sumPar += x;
        <span class="kw">else</span> prodImpar *= x;
    &#125;
    fout &lt;&lt; <span class="st">"Suma pare: "</span>  &lt;&lt; sumPar &lt;&lt; endl;
    fout &lt;&lt; <span class="st">"Prod impar: "</span> &lt;&lt; prodImpar;
    <span class="kw">return</span> <span class="nu">0</span>;
&#125;</pre>
      </div>

      <div class="callout purple" style="margin-top:20px;">
        <strong>Problemă 2:</strong> Fișierul <code>text.in</code> conține un text pe mai multe linii. Scrieți în <code>text.out</code> fiecare cuvânt de lungime maximă din text (câte unul pe linie).
      </div>
      <div class="callout success">
        <strong>Soluție:</strong>
        <pre style="margin:8px 0 0;background:transparent;border:none;padding:0;"><span class="kw">#include</span> <span class="st">&lt;fstream></span>
<span class="kw">#include</span> <span class="st">&lt;vector></span>
<span class="kw">#include</span> <span class="st">&lt;sstream></span>
<span class="kw">using namespace</span> std;
<span class="kw">int</span> <span class="fn">main</span>() &#123;
    ifstream fin(<span class="st">"text.in"</span>);
    ofstream fout(<span class="st">"text.out"</span>);
    vector&lt;string> toate;
    string linie, cuvant;
    <span class="kw">int</span> lenMax = <span class="nu">0</span>;
    <span class="kw">while</span> (getline(fin, linie)) &#123;
        stringstream ss(linie);
        <span class="kw">while</span> (ss >> cuvant) &#123;
            toate.push_back(cuvant);
            <span class="kw">if</span> ((<span class="kw">int</span>)cuvant.size() > lenMax) lenMax = cuvant.size();
        &#125;
    &#125;
    <span class="kw">for</span> (<span class="kw">auto</span> &w : toate)
        <span class="kw">if</span> ((<span class="kw">int</span>)w.size() == lenMax) fout &lt;&lt; w &lt;&lt; endl;
    <span class="kw">return</span> <span class="nu">0</span>;
&#125;</pre>
      </div>

      <div class="callout info" style="margin-top:20px;">
        <strong>Resurse utile BAC informatică:</strong><br>
        <a href="https://pbinfo.ro" target="_blank" rel="noopener">pbinfo.ro</a> — probleme și soluții BAC •
        <a href="https://infoarena.ro" target="_blank" rel="noopener">infoarena.ro</a> — arhivă olimpiadă •
        <a href="https://cplusplus.com/reference/fstream/" target="_blank" rel="noopener">cplusplus.com/reference/fstream</a> — referință fstream
      </div>
    </div>
  </div>
</section>

<script is:inline>
window.QUIZ_DATA = window.QUIZ_DATA || {};
window.QUIZ_DATA['fisiere-quiz'] = [
  {
    "q": "Ce header trebuie inclus pentru lucrul cu fișiere în C++?",
    "opts": [
      "<stdio.h>",
      "<fstream>",
      "<files.h>",
      "<io.h>"
    ],
    "ans": 1,
    "exp": "#include <fstream> oferă ifstream (citire), ofstream (scriere) și fstream (ambele).",
    "diff": 1
  },
  {
    "q": "Cum deschidem un fișier 'date.in' doar pentru citire?",
    "opts": [
      "ofstream f('date.in');",
      "ifstream f('date.in');",
      "fstream f('date.in', ios::out);",
      "FILE* f = fwrite('date.in');"
    ],
    "ans": 1,
    "exp": "ifstream = input file stream. Implicit se deschide în modul citire.",
    "diff": 1
  },
  {
    "q": "Cum verificăm dacă fișierul s-a deschis cu succes?",
    "opts": [
      "if (f == true)",
      "if (f.is_open())",
      "if (f != NULL)",
      "if (f.good() == 1)"
    ],
    "ans": 1,
    "exp": "f.is_open() returnează true dacă fișierul a fost deschis corect — esențial pentru robustețe.",
    "diff": 2
  },
  {
    "q": "freopen('date.in', 'r', stdin) face ca:",
    "opts": [
      "stdin să fie redirectat la fișier (cin citește din fișier)",
      "Fișierul să fie creat gol",
      "Date să fie copiate în stdin",
      "freopen nu există în C++"
    ],
    "ans": 0,
    "exp": "freopen redirectează stdin → tot codul care citește cu cin sau scanf va citi din fișier.",
    "diff": 1
  },
  {
    "q": "Ce se întâmplă cu datele existente dacă deschidem un fișier cu ofstream fara ios::app?",
    "opts": [
      "Se păstrează",
      "Se șterg (fișierul e trunchiat)",
      "Se adaugă la final",
      "Apare eroare"
    ],
    "ans": 1,
    "exp": "ofstream deschide implicit în ios::trunc → conținutul anterior este șters. Folosiți ios::app pentru adăugare.",
    "diff": 2
  },
  {
    "q": "Cum citim o linie întreagă (inclusiv spații) dintr-un fișier f?",
    "opts": [
      "f >> linie;",
      "getline(f, linie);",
      "f.read(linie, 100);",
      "fscanf(f, '%s', linie);"
    ],
    "ans": 1,
    "exp": "f >> linie; se oprește la spații. getline(f, linie) citește până la newline inclusiv spații.",
    "diff": 3
  },
  {
    "q": "Funcția f.eof() returnează true când:",
    "opts": [
      "Fișierul este gol",
      "Am citit dincolo de ultimul caracter",
      "Fișierul este deschis",
      "Scrierile au eșuat"
    ],
    "ans": 1,
    "exp": "eof() (end of file) devine true după ce s-a încercat o citire DINCOLO de finalul fișierului.",
    "diff": 2
  },
  {
    "q": "La ce se folosește ios::app la deschiderea unui fișier?",
    "opts": [
      "Să citim de la final",
      "Să adăugăm date fără a șterge conținutul existent",
      "Să deschidem în mod binar",
      "Să citim și scriem simultan"
    ],
    "ans": 1,
    "exp": "ios::app (append) mută cursorul la final înainte de fiecare scriere → conținutul vechi e păstrat.",
    "diff": 3
  },
  {
    "q": "Care este ordinea corectă la lucrul cu fișiere?",
    "opts": [
      "Deschide → Verifică → Prelucrează → Închide",
      "Prelucrează → Deschide → Închide",
      "Verifică → Deschide → Prelucrează",
      "Deschide → Prelucrează → Verifică → Închide"
    ],
    "ans": 0,
    "exp": "Buna practică: deschide, verifică dacă s-a deschis, prelucrează datele, închide fișierul.",
    "diff": 2
  },
  {
    "q": "În C++, ifstream și ofstream se închid automat când:",
    "opts": [
      "Apelăm explicit close()",
      "Obiectul iese din scope (destructorul se apelează)",
      "Ambele variante de mai sus sunt corecte",
      "Fișierul este gol"
    ],
    "ans": 2,
    "exp": "Obiectele fstream au destructori RAII care închid fișierul, DAR este bună practică să apelăm .close() explicit.",
    "diff": 3
  },
  {
    "q": "(BAC) Pentru a deschide fișierul 'date.in' la citire în C++ folosim:",
    "opts": [
      "ofstream f('date.in')",
      "ifstream f('date.in')",
      "fstream f('date.in', ios::in)",
      "FILE *f=fopen('date.in','w')"
    ],
    "ans": 1,
    "exp": "ifstream = input file stream — pentru citire. ofstream = output (scriere).",
    "diff": 1
  },
  {
    "q": "Metoda de verificare că un fișier s-a deschis corect:",
    "opts": [
      "f.good()",
      "f.open()",
      "f.check()",
      "f.exists()"
    ],
    "ans": 0,
    "exp": "f.good() sau if(!f) returnează starea fluxului — convenție uzuală în C++.",
    "diff": 2
  },
  {
    "q": "(BAC 2021) freopen('rezultate.out','w',stdout) permite:",
    "opts": [
      "Citirea din fișier cu cin",
      "Scrierea cu cout direct în fișier",
      "Copierea unui fișier",
      "Ștergerea fișierului"
    ],
    "ans": 1,
    "exp": "freopen redirecționează stdout → orice cout scrie în 'rezultate.out'.",
    "diff": 2
  },
  {
    "q": "Ce se întâmplă dacă nu se apelează f.close()?",
    "opts": [
      "Eroare de compilare",
      "Buffer-ul poate să nu fie scris pe disc (flush)",
      "Fișierul se șterge",
      "Nimic, C++ se ocupă automat întotdeauna"
    ],
    "ans": 1,
    "exp": "Fără close(), datele din buffer pot rămâne nescrise. Destructorul face flush, dar e bună practică să închizi explicit.",
    "diff": 2
  },
  {
    "q": "(BAC 2023) Citirea tuturor numerelor dintr-un fișier până la EOF:",
    "opts": [
      "while(f>>x) suma+=x;",
      "for(int i=0;i<n;i++) f>>x;",
      "while(!f.eof()) {f>>x; suma+=x;}",
      "do{f>>x;}while(f)"
    ],
    "ans": 0,
    "exp": "while(f>>x) este idiomul corect: citește și verifică succesul simultan, evitând citirea dublă a ultimului element.",
    "diff": 3
  },
  {
    "q": "Diferența între ios::app și ios::trunc la deschiderea unui fișier:",
    "opts": [
      "app=adaugă la final, trunc=șterge conținutul",
      "app=șterge, trunc=adaugă",
      "app=citire, trunc=scriere",
      "Sunt identice"
    ],
    "ans": 0,
    "exp": "ios::app (append) = scrie la sfârșitul fișierului existent; ios::trunc = șterge conținutul înainte de scriere.",
    "diff": 2
  },
  {
    "q": "(BAC dificil) Citirea unei linii întregi (cu spații) dintr-un fișier text se face cu:",
    "opts": [
      "f>>linie",
      "f.get(linie)",
      "getline(f,linie)",
      "f.read(linie,100)"
    ],
    "ans": 2,
    "exp": "getline(f, linie) citește până la newline inclusiv spațiile — f>>linie se oprește la primul spațiu.",
    "diff": 3
  }
];
</script>
</ChapterLayout>
